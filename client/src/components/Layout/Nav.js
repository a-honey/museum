import { styled } from "styled-components";

const NavBlock = styled.div`
  background-color: yellow;
  width: 100%;
  height: 200px;
`;

const Nav = () => {
  return <NavBlock />;
};

export default Nav;
